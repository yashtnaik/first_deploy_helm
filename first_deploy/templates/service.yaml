# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ include "first_deploy.fullname" . }}
#   labels:
#     {{- include "first_deploy.labels" . | nindent 4 }}
# spec:
#   type: {{ .Values.service.type }}
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: http
#       protocol: TCP
#       name: http
#   selector:
#     {{- include "first_deploy.selectorLabels" . | nindent 4 }}
